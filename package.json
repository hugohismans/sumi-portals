{
  "name": "sumi-portals",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "check": "esbuild src/core/__check.ts --bundle --platform=node --format=esm --outfile=node_modules/.tmp/check.mjs --log-level=error && node node_modules/.tmp/check.mjs"
  },
  "dependencies": {
    "firebase": "^12.17.0",
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
